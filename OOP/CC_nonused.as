package
{
	public class CC_nonused
	{
		
		while (true)
		{
			enemies[i].isDead();
			enemies[i].isOnRange();
			this.addEventListener(Event.Added,beginclass)
		}
		
		/*
		private var enemies:Array;
		private var towers:Array;
		private var game:Game;
		private var door:Door;
		
		public function CC_nonused(enemies:Array , towers:Array , game:Game, door:Door)
		{
			this.enemies = enemies;	
			this.towers = towers;
			this.game = game;
			this.door = door;
		}
		
		/*public function CC_nonused(quantity_e:int, quantity_t:int, game:Game, door:Door)
		{
			this.game = game;
			this.door = door;
			this.enemies = new Array[quantity_e];
			this.towers = new Array[quantity_t]
		}
		
		public function start()
		{
			game.start();
			
			update();
		}
		
		public function update()
		{
			if (door.isBroken())
				gameOver();
		}
				
		public function gameOver()
		{
			for (var i:int = 0; i < enemies.length; i++){
				if (enemies[i] != null)
					enemies[i].kill();
			}
		}
		
		public function shoot(tower:Tower, enemies:Array)
		{
			tower.shoot(enemies);					
		}
	} */
}